# Algorithms Playground

Open `index.html` in a browser (no server required) to run the site.

Features:
- Choose Java or JavaScript display
- See the six core algorithms and their source code
- Open an execution modal with dedicated input fields
- Dark accessible palette and simple animations

Note: The site displays Java and JS source code but runs equivalent JavaScript implementations for execution inside the browser.
